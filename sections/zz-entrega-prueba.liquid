{%- comment -%}SONDA TEMPORAL de entrega-habil (se borra al terminar). No la enlaza ninguna plantilla.{%- endcomment -%}
{%- assign formatos = 'rango,entre,entre_b,otro' | split: ',' -%}
<pre id="zz-entrega-prueba">
{%- for f in formatos -%}
{%- capture o -%}{% render 'entrega-habil', formato: f, ahora: 1789750800 %}{%- endcapture -%}
S|{{ f }}|{{ o | json }}
{% endfor -%}
{%- capture o -%}{% render 'entrega-habil', formato: 'rango', ahora: 1789750800, dias_min: 1, dias_max: 1 %}{%- endcapture -%}
S|min1max1|{{ o | json }}
</pre>
{% schema %}{"name": "zz prueba entrega"}{% endschema %}
